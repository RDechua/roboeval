# Spatial perturbation cell: cube initial y-position shifted +1 cm.
# PRD §6.4 spatial axis, intensity 1.
# Inherits seeds, max_steps, success block, policy, env from act_nominal.yaml.
# Wall on M1 MPS: ~20-25 minutes per cell (3 seeds × 50 rollouts).
extends: configs/baseline/act_nominal.yaml

perturbation:
  kind: spatial
  dx_m: 0.0
  dy_m: 0.01

wandb:
  name_prefix: act_spatial_y+1cm
  tags: [week4, perturbation, spatial, act, y+1cm]
