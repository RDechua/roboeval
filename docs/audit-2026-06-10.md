# RoboEval Pre-Ship Audit — 2026-06-10

Pre-v1.0 internal-consistency + live-infrastructure audit. Read-only checks
plus a small set of typo-level inline fixes (listed at the bottom). Nothing
that changes scope, gate status, the demo-video descope, the G3 deferral, or
runtime data was touched without sign-off — those are in **Needs decision**.

## Verdict

**FIX-FIRST** — the project is functionally ship-ready (all four gates green,
all live URLs 200, CI green, MkDocs builds under `--strict`, headline data
consistent). Two unambiguous defects were fixed inline and committed. Three
accuracy/structure items need your call before tagging v1.0; none block
function.

---

## Consistency findings

| # | Location | Issue | Disposition |
|---|---|---|---|
| C1 | `docs/STATE.md:107` | Claimed "34 source files in `roboeval/` + `scripts/`, 245 tests passing". Actual: 34 in `roboeval/` + 7 in `scripts/` = **41**; **285** tests pass (3 deselected, optional-dep gated). | ✅ Fixed inline (`7d2a7a8`) |
| C2 | `docs/PRD.md:356` (§8.2) | Spec says action composition uses a **learnable** α "initialised at 0.1 with sigmoid clipping". Phase 4 actually shipped a **fixed** α = 0.05; co-trainable α is explicitly the v1.1 #1 priority (not built). The spec overstates what shipped. | ⚠ Needs decision (PRD rewrite) |
| C3 | `docs/PRD.md:334-335` (§7.3) | Protocol specifies "30 rollouts (5 per category)" and a separate `manual_labels_{run_sha}.json`. Implementation exported **35** (18 at −5 cm + 17 at +5 cm) and embeds `manual_failure_mode` inside the `relabel_sample_*.json` files. Minor numeric/structural drift; protocol intent intact. | ⚠ Needs decision (PRD), low priority |

Descope + deferral are internally consistent everywhere they appear:
STATE.md §Phase + Next-session item 4 (descoped), PRD §3.2 non-goals (line 72),
PRD §9.2 G5 row (video clause removed), research-log Week 9 (2026-06-10 G3
deferral). No doc claims G3 closed; PRD §7.3 correctly remains an open gate.

---

## Dead reference findings

| Location | Hit | Classification |
|---|---|---|
| `docs/research-log.md:185` | "…the PRD's '90-second demo video' deliverable (Section 9)…" | **(b) Intentional historical.** Week-4 journal entry, accurate for its date. Research-log is a frozen week-by-week record; not rewritten. |
| `docs/PRD.md:72`, `docs/STATE.md:26,281` | "90-second demo video … descoped" | **Correct** — these are the descope notes themselves, not stale references. |

No surviving live references to `demo-video-script`, `Sunday κ relabel`,
`watch the rollout video`, or `2026-05-24 unlock` as actionable instructions
outside the frozen `docs/superpowers/` artifacts. `docs/blog/demo-video-script.md`
is confirmed deleted and stays deleted.

---

## URL casing findings

| Location | Issue | Disposition |
|---|---|---|
| `pyproject.toml:60-61` | `github.com/RubenoDechua/roboeval` → wrong; canonical is `RDechua`. Ships in package metadata. | ✅ Fixed inline (`ca96abc`) |
| `docs/superpowers/**` | ~21 mixed-case `RubenoDechua` URLs (8 GitHub + 13 HF) | **No fix** — frozen historical artifacts; reference memory explicitly says these are intentionally not updated. |

Verified correct across the live docs: 20× `github.com/RDechua/roboeval`,
`rubenodechua` (lowercase) on all HF URLs, `rdechua.github.io` for Pages.
(Note: a naive `grep -ho | grep -v superpowers` *appears* to show 21 violations
in tracked docs — that is a grep artifact; `-h -o` strips the path so the
path-exclude no-ops. A path-keeping case-sensitive grep confirms the only
non-frozen hits were the two in `pyproject.toml`.)

---

## Code-vs-docs findings

| # | Location | Issue | Disposition |
|---|---|---|---|
| D1 | `README.md:36`, `docs/PRD.md:144` (§5.2) | Layout blocks show `configs/.../residual_rl/`, but the real, tracked Phase-4 configs live in `configs/residual/`. `configs/residual_rl/` is a **vestigial empty dir** (only `.gitkeep`). | ⚠ Needs decision (see below) |
| D2 | `docs/PRD.md` §5.2/§5.4 | Idealized artifact tree lists `data/runs/`, `data/residual/`, `analysis/figures/`, `analysis/notebooks/`. Actual `data/` holds only `calibration/` + `taxonomy/`. Spec describes an aspirational layout. | ⚠ Needs decision (PRD), low priority |
| — | `roboeval/dashboard/data.py` ↔ `data/headline.json` | **Match.** `headline.json` is schema v2 with all 10 cells carrying every field `data.py` reads (`cell_id, axis, magnitude, mean_tsr_custom, std_tsr_custom, per_seed_tsr_custom, mean_tsr, median_tts, failure_counts, n_rollouts, run_id`); loader accepts `{1, 2}`. | ✅ No action |
| — | `mkdocs.yml` nav | All nav targets exist and all internal links resolve — `--strict` passed, which fails on either. | ✅ No action |
| — | `roboeval/` package layout (`envs/policies/evaluation/taxonomy/residual`) | Matches PRD §5.2 description. | ✅ No action |
| — | README Quick Start | Spot-checked (commands reference real `roboeval smoke` CLI + correct LeRobot import path); **not executed end-to-end** on a fresh clone. | ℹ Note |

---

## Gate status

All four gates **PASS** (run 2026-06-10):

```
$ ruff check .
All checks passed!

$ ruff format --check .
89 files already formatted

$ mypy --strict roboeval
Success: no issues found in 34 source files

$ pytest -q
285 passed, 3 deselected in 4.79s
```

---

## MkDocs build status

`mkdocs build --strict` → **exit 0**, "Documentation built in ~1 s". No
warnings. (The red block in output is Material's upstream MkDocs-2.0 advisory
banner, not a build warning.)

One **INFO** (non-fatal): `kappa-relabel-runbook.md` exists in `docs/` but is
not in nav — it is published to the site yet unlinked (orphaned). It is an
internal "won't run yet" runbook; recommend adding it to `mkdocs.yml`
`exclude_docs:` alongside `STATE.md`/`research-log.md`. Left for your call
(see Needs decision). This audit file (`audit-2026-06-10.md`) **was** added to
`exclude_docs` so it does not publish.

---

## Live infra status

| URL | HTTP |
|---|---|
| https://github.com/RDechua/roboeval | **200** |
| https://rdechua.github.io/roboeval/ | **200** |
| https://rubenodechua-roboeval.hf.space/ | **200** |
| https://huggingface.co/spaces/rubenodechua/roboeval | **200** |

---

## CI status

`gh run list --branch main --limit 5` — latest push (`837def7`, the G3
deferral) is **green on both jobs**: `CI` (1m17s) ✓ and `Docs` (23s) ✓. No
failures in the last 5 runs.

---

## Headline data status

**Consistent.** Re-running `python -m scripts.build_headline_json` reproduces
`data/headline.json` byte-for-byte except the `generated_at` timestamp
(`2026-05-21T21:28:33Z` committed → today on rebuild). All cell, ablation, and
Welch-test data is identical. The committed copy was restored after the test.

Minor: the builder has no `--check`/verify subcommand and stamps a
non-deterministic `generated_at`, so a plain `git diff` can't be used as a CI
freshness gate without normalizing that field. Not blocking.

---

## Needs decision

These are yours — I did not act on them.

1. **PRD §8.2 — learnable vs fixed α (C2).** The spec still describes a
   learnable α (init 0.1, sigmoid-clipped); v1.0 shipped fixed α = 0.05 with
   co-trainable α deferred to v1.1. Rewrite §8.2 to match what shipped (with a
   forward-pointer to the v1.1 item), or leave it as forward-looking design?
2. **`configs/residual_rl/` vestigial dir + docs (D1).** Three coupled options:
   (a) delete the empty `configs/residual_rl/.gitkeep` dir and update
   README §Layout + PRD §5.2 to `residual/`; (b) keep `residual_rl/` as the
   canonical name and move the two real configs into it; (c) leave as-is.
   Touches a tracked dir + PRD, so I held off.
3. **`docs/PRD.md` §5.2/§5.4 idealized artifact tree (D2)** and **the orphaned
   `kappa-relabel-runbook.md` in the public site.** Do you want the PRD tree
   reconciled to actual `data/` contents, and the runbook excluded from the
   published site (like STATE.md) or left public with its "won't run" banner?

---

## Inline fixes applied

| Commit | Fix |
|---|---|
| `7d2a7a8` | `docs/STATE.md` — source-file count 34→41 (34 + 7), test count 245→285 |
| `ca96abc` | `pyproject.toml` — project URLs `RubenoDechua`→`RDechua` (canonical GitHub) |
| _(this doc's commit)_ | Added `docs/audit-2026-06-10.md` + excluded it from the MkDocs site |

All commits authored `Rubeno Dechua <rubenodechua123@gmail.com>`, no AI trailers.
